package com.amazon.gdpr;


import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

import java.util.List;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.annotation.DirtiesContext;
import org.springframework.test.context.junit4.SpringRunner;


import com.amazon.gdpr.processor.BackupTableProcessor;
@SpringBootTest
public class GDPRApplicationTests {
	/*
	 * @Autowired GdprController gdprController;
	 * 
	 * @Autowired private BackupTableProcessor backupTableProcessor; int runId = 0;
	 * List<AnonymizeDetails> activeAnonymizeDtlsList=null;
	 * 
	 * @Test public void backupTableTest() { runId=
	 * gdprController.initiateRun("Test Run"); boolean
	 * backupStaus=backupTableProcessor.processBkpupTable(runId,
	 * activeAnonymizeDtlsList); assertTrue(backupStaus);
	 * 
	 * }
	 * 
	 * @Test public void backupTableCheckTest() { Boolean bkpupTblProcessStatus =
	 * false; bkpupTblProcessStatus =
	 * backupTableProcessor.bkpupTableCheck(activeAnonymizeDtlsList);
	 * assertTrue(bkpupTblProcessStatus);
	 * 
	 * }
	 * 
	 * @Test public void sum_with3numbers() { System.out.println("Test1"); int fg=6;
	 * assertEquals(6, fg); }
	 */
	
}
